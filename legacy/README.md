# Legacy scripts

v2.0 (bash, PowerShell) and v2.1 (bash) kept for reference only. They are
unmaintained and have known defects: no A/B slot handling, no init_boot
support, unconfirmed bootloader unlock, host-side execution of Android
binaries. Use the `sudroid` CLI instead.

Removed in v3.1.
